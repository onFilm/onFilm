import { ItalicTextDirective } from './italic-text.directive';

describe('ItalicTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ItalicTextDirective();
    expect(directive).toBeTruthy();
  });
});
